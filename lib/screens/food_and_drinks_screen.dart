import 'package:flutter/material.dart'; 

class FoodAndDrinksScreen extends StatelessWidget {
  const FoodAndDrinksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FoodsAndDrinks'),
      ),
    );
  }
}
